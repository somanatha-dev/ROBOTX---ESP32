// The real sketch, compiled for the host simulation. Nothing is copied: the
// .ino is included as-is, and its own #include "config.h" etc. resolve
// relative to the sketch directory.
#include "../../robot_controller.ino"
